# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json

# Seed CSVs (generated, not source-controlled)
seeds/csv/

# Python
__pycache__/
*.pyc
.venv/
dist/
*.egg-info/

# Node
node_modules/

# macOS
.DS_Store

# AWS credentials — never commit these
.env
*.env
credentials
.account_id
.tf_outputs
*.tfplan
tfplan

# Deploy artifacts / logs
deploy.log
api/.build/
api/lambda.zip
