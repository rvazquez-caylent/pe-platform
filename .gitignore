# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json

# Seed CSVs (generated, not source-controlled)
seeds/csv/

# Python
__pycache__/
*.pyc
.venv/
dist/
*.egg-info/

# Node
node_modules/

# AWS credentials — never commit these
.env
*.env
credentials
